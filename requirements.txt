# Pinned to the versions this was actually built and tested against.
# Unpinned, Railway installs whatever is newest: Streamlit is already several
# minor versions ahead, and this UI depends on version-sensitive behaviour
# (the chat input only pins to the viewport bottom when it has no ancestor
# block, and the theme's inline font loading is 1.50+).
streamlit==1.50.0
requests==2.32.5
google-generativeai==0.8.6
python-dotenv==1.2.1
pillow==11.3.0
